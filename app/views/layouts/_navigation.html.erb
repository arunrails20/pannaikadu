<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
  <div class="container">
    <a class="navbar-brand" href="/">pannaikadu.com</a>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav ml-auto">
      <% Refinery::Page.where(parent_id: nil).each do |page| %>
        <li class="nav-item">
          <%= link_to page.slug, page.slug, :class => "nav-link" %>
        </li>
      <% end %>
      </ul>
    </div>
  </div>
</nav>